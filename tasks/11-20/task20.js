'use strict';

const currentDate = new Date();

console.log(currentDate);

const currentDateInMs = currentDate.getTime();

console.log(currentDateInMs);

console.log(typeof currentDate);
console.log(typeof currentDateInMs);
